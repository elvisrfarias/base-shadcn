import { Metadata } from "next";

export const metadata: Metadata = {
	title: "Configurações | Athus",
	description: "Tela de configurações da Athus",
};

const Settings = () => {
	return (
		<div>
			settings
		</div>
	)
}

export default Settings
